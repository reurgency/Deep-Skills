<!-- GENERATED — do not hand-edit; regenerate overwrites. Source: deep-docs. -->
<!-- tier-0 map: the always-loaded entry point. One line per subsystem (or group). Keep tiny. -->

# <repo name> — AI map

> Indexed against `<git SHA or "non-git">` · <N> subsystems · generated by deep-docs.
> This is the orientation layer. Load a subsystem card only when its **load-when** matches your task.
> Token figures are **estimates** (`~N est`, chars/4); tier-0 is held under a hard ceiling (~2,000 est) — see `references/token-budget.md`.
> Machine index: [`index.json`](./index.json) · Coverage & gaps: [`coverage.md`](./coverage.md)

<!--
TWO RENDERINGS — pick one per generation (see references/token-budget.md):
  (A) FLAT     — when the flat subsystem table keeps tier-0 ≤ ~2,000 est. The default.
  (B) GROUPED  — when the flat table would push tier-0 over the ceiling: collapse subsystems into
                 top-level-area groups with per-group SUBTOTALS + a group load-when; per-subsystem
                 detail spills into index.json (the grouped index), never truncated; every group is
                 logged in coverage.md's boundary-decisions section.
Every token figure on either rendering carries the "~N est" estimate label — no exceptions.
-->

## Subsystems  <!-- rendering (A): FLAT -->

| Subsystem | Card | ~tokens (est) | Load when |
|---|---|---|---|
| `<auth>` | [subsystems/auth.md](./subsystems/auth.md) | ~450 est | working inside auth/session |
| `<billing>` | [subsystems/billing.md](./subsystems/billing.md) | ~320 est | touching charges, invoices, or webhooks |

<!--
## Subsystems (grouped)  // rendering (B): GROUPED — over the ceiling, one row per top-level area.
// Each row is a GROUP with a SUBTOTAL (sum of member token_est, ~N est) + a group-level load-when.
// Per-member cards/token-est/load-when live in index.json — expand a group via the index.

| Group (top-level area) | Members | ~tokens (est) | Load when |
|---|---|---|---|
| `services/` | 14 cards — see index.json | ~6.8k est | anything under services/ — billing, notifications, or search |
| `web/`      |  9 cards — see index.json | ~4.1k est | building or fixing UI under web/ |
| `other`     | 23 small cards — see index.json | ~3.0k est | low-traffic areas; consult index.json by concept |
-->

## External references (not anchor-verified)

Existing human-authored docs, indexed as pointers only — never ingested into a trusted tier.

| Doc | ~tokens (est) | Load when |
|---|---|---|
| [docs/roadmap/DESIGN-OUTLINE.md](../../docs/roadmap/DESIGN-OUTLINE.md) | ~1.2k est | need product/design background (human-authored · not anchor-verified) |

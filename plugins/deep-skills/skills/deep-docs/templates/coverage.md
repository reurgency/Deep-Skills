<!-- GENERATED — do not hand-edit; regenerate overwrites. Source: deep-docs. -->
<!-- The doc-coverage honesty artifact. Distinct from the State/Data-Flow contract:
     coverage = what got documented and what didn't; contract = the skill's own state writes. -->

# Coverage — <repo name>

> Indexed against `<git SHA or "non-git">` · generated by deep-docs.
> Honest record of what was mapped, what wasn't, and why. Silence never implies completeness.

## Documented

| Subsystem | Card | Tier-2 ref? |
|---|---|---|
| `<auth>` | subsystems/auth.md | yes — references/auth.md (cross-layer login flow) |
| `<billing>` | subsystems/billing.md | no (card-only) |

## Card-only (no tier-2 ref earned)

Subsystems that got a card but no deep reference — a recorded decision, not an omission (no real cross-layer flow or non-obvious invariant warranted one):

- `<billing>` — straightforward CRUD over invoices; no cross-layer trace needed.

## Not documented + why

The explicit not-documented list (surfaced in the final report):

- `<scripts/>` — build tooling, out of scope for the orientation map.
- `<legacy/>` — deferred; flagged for a follow-up run.

## Referenced-vs-ingested (existing-docs quarantine audit)

Existing human-authored docs indexed as `external-unverified` pointers only — **none ingested** into a trusted tier:

| Doc | Action |
|---|---|
| docs/roadmap/DESIGN-OUTLINE.md | referenced as pointer (human-authored · not anchor-verified) — not ingested |

## Boundary decisions

Every split/merge/cap/"other"-grouping made during survey:

- split `src/ui/` (>50 files) by entry-point into `ui-shell`, `ui-components`.
- merged `src/util/` (<3 files) into its parent boundary.
- capped at ~12 boundaries; grouped the 3 smallest into `other`.

## Drift (from anchor-verify)

Anchors that came back `drifted` (these blocked publish until their subsystem was regenerated + re-verified):

- <none this run> *(or: `src/auth/mfa.ts (verifyTotp)` — symbol gone; auth regenerated.)*

## Capped boundaries (whole-repo scale cap)

When a whole-repo run was interactively scoped, what was deferred and why:

- <none — full repo mapped> *(or: deferred `packages/experimental/*` per user; map the rest now.)*
